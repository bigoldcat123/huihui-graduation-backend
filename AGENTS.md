# Agent Conventions

- Use Bun for all package manager and script commands.
- Prefer `bun install` and `bun run <script>`.
- Do not use `npm`, `yarn`, or `pnpm` commands unless explicitly requested by the user.
